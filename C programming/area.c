#include<stdio.h>
int main(){
    int a,b;
    printf("Enter the length of the square: ");
    scanf("%d", &a);

    printf("Enter the width of the square: ");
    scanf("%d", &b);

    printf("The area of the square is: %d", a * b);


return 0;    
}