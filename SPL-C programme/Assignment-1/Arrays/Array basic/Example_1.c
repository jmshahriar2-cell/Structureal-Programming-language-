#include <stdio.h>

int main() {
    // Initialize an integer array named myNumbers with four values
    int myNumbers[] = {25, 50, 75, 100};
    
    // Access the first element of the array using its index number[cite: 9]
    // Because array indexes start with 0, myNumbers[0] refers to the first element[cite: 9]
    printf("%d", myNumbers[0]); // Outputs 25[cite: 9]
    
    return 0;
}