#include <stdio.h>

int main() {
    // Declare two int variables named age and minAge, and assign them values
    int age = 20; 
    int minAge = 18; 

    // Use printf to print the result of this comparison: age >= minAge
    // This will print 1 if true, or 0 if false
    printf("%d\n", age >= minAge);

    return 0;
}

